create procedure [EODData].[EndTask](@PoolID smallint, @TaskID int, @Error varchar(max))
as
begin
	set nocount on

	--PoolID, TaskID, MethodName, IntervalID, Exchange, Symbol, DateFrom, DateTo, EnlistDate, StartDate, Retries, Error
end